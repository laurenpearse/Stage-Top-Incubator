# Scripts

Analysis and plotting scripts (Python/MATLAB) that turn raw logs in `../data/logs/` into the processed results in `../data/characterization/`. Keep scripts reproducible — note the expected input file(s) and what each script outputs at the top of the file.
